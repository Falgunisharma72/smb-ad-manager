# Copy to .env.local for local dev. DO NOT commit .env.local.

# HF Space API backend (public, no auth needed)
NEXT_PUBLIC_API_URL=https://falgunisharma-smb-ad-manager.hf.space

# Demo login credentials. Change the password before sharing the URL!
AUTH_USERNAME=admin
AUTH_PASSWORD=hackathon2026
