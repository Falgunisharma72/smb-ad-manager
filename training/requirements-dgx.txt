# DGX-compatible training deps — for nvcr.io/nvidia/pytorch:22.12-py3
# on NVIDIA driver 470 (via CUDA 11.8 forward-compat).
#
# CRITICAL: do NOT install torch/torchvision here. NVIDIA's pre-built
# torch 1.14.0a0 has the forward-compat libs baked in; upgrading it
# kills CUDA access.
#
# Install inside the container with:
#   pip install -r training/requirements-dgx.txt

# Pinned to Python-3.8-compatible versions that don't trigger torch upgrade.
transformers==4.40.2
peft==0.10.0
datasets==2.14.7
accelerate==0.30.1

# Logging + HTTP
tensorboard
requests>=2.31

# Tokenizer library explicitly pinned to match transformers 4.40
tokenizers>=0.19,<0.20
safetensors>=0.4.0
